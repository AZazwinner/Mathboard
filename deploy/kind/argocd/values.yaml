# A small Argo CD: no SSO (Dex), no notifications, no ApplicationSet controller. The UI is reached
# through kubectl port-forward, so it runs without TLS.
global:
  domain: localhost

configs:
  params:
    server.insecure: true

dex:
  enabled: false
notifications:
  enabled: false
applicationSet:
  replicas: 0
