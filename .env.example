# Used by docker-compose.yml. Copy to .env to override the dev defaults.
# Never commit the real .env (already gitignored).

# POSTGRES_PASSWORD=mathboard-dev
# SECRET_KEY=generate one with: openssl rand -hex 32
# CORS_ORIGINS=
